"use client";

import { Application } from "@/lib/prisma";

export default function ApplicationComponent({ application }: { application: Application }) {
  return (
    <div className="flex items-center bg-gray-200 dark:bg-gray-800">
    </div>
  );
}
